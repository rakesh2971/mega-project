{
  "name": "neuromate-backend",
  "version": "1.0.0",
  "description": "NeuroMate Backend API with Supabase (PostgreSQL)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --import ./instrument.js server.js",
    "dev": "nodemon --experimental-specifier-resolution=node server.js"
  },
  "dependencies": {
    "@sentry/node": "^8.0.0",
    "@sentry/profiling-node": "^8.0.0",
    "@supabase/supabase-js": "^2.47.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
